# 秒哒创建指南

## 方案 A：推荐 - 直接使用独立 HTML 文件

`index-miaoda.html` 是已经准备好的完整网站文件，可以在任何支持静态网页的平台部署：

### 部署到国内平台的选择

1. **Gitee Pages**（推荐）
   - 创建 Gitee 仓库，上传 `index-miaoda.html`（重命名为 `index.html`）
   - 在仓库设置中开启 Gitee Pages
   - 获得 `xxx.gitee.io` 域名

2. **Coding Pages**（腾讯云）
   - 在 Coding 创建项目，上传文件
   - 开启静态网站托管
   
3. **直接在本地使用**
   - 双击 `index-miaoda.html` 直接用浏览器打开即可使用

### 使用说明

1. 打开网站后，进入「设置」页面
2. API 地址已默认填好：`https://my-device-control-web.pages.dev/api`
3. 输入你的 API 密钥（与 Cloudflare Pages 环境变量 API_KEY 一致）
4. 点击「保存设置」，然后测试连接

---

## 方案 B：在秒哒上创建（功能受限）

秒哒是 AI 生成平台，无法上传自定义 HTML。但你可以描述需求让它生成类似的应用。

### 粘贴到秒哒的需求描述

```
请生成一个"设备管理控制台"Web应用，暗色主题（深灰+蓝色），响应式设计。

核心功能：
1. 仪表板：显示桌面应用在线状态（通过心跳API检测），显示设备数量、工作区数量、活跃任务数
2. 快捷操作：选择工作区后，执行同步代码、强制同步、构建、Cherry-Pick、自定义命令等操作
3. 远程终端：选择设备后执行SSH命令，显示命令输出历史
4. 文件浏览：选择设备后浏览远程目录，支持路径导航、面包屑
5. CI工具：Jenkins作业列表和构建触发，Gerrit变更搜索
6. 命令历史：查看所有命令的执行状态和结果，支持按状态筛选
7. 工作区管理：查看、添加、删除工作区
8. 设置：配置API地址和密钥

技术要求：
- 所有功能通过REST API与后端通信
- API地址可配置（默认：https://my-device-control-web.pages.dev/api）
- 每个请求携带X-API-Key请求头
- 支持localStorage保存设置
- 左侧固定导航栏，移动端底部标签栏
```

注意：秒哒生成的应用不会有真实的API集成，需要手动修改代码添加。
